<Window x:Class="MyWireGuard.App.Dialogs.FileSendProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="发送文件"
        Width="420"
        Height="180"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TargetTextBlock"
                   FontSize="14"
                   FontWeight="SemiBold" />
        <TextBlock x:Name="FileTextBlock"
                   Grid.Row="1"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap" />
        <ProgressBar x:Name="TransferProgressBar"
                     Grid.Row="2"
                     Height="18"
                     Margin="0,16,0,0"
                     Minimum="0"
                     Maximum="100" />
        <TextBlock x:Name="ProgressTextBlock"
                   Grid.Row="3"
                   Margin="0,12,0,0"
                   HorizontalAlignment="Right" />
    </Grid>
</Window>
