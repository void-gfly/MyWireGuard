<Window x:Class="MyWireGuard.App.Dialogs.UnhandledExceptionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="程序异常"
        Width="760"
        Height="520"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Background="{StaticResource PanelElevatedBrush}"
        FontFamily="Microsoft YaHei UI"
        ThemeMode="None">
    <Border Margin="14"
            Padding="20"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="程序发生未处理异常"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" />

            <TextBlock Grid.Row="1"
                       Margin="0,10,0,12"
                       Text="请复制下面的异常信息或发送本地 crash 日志用于定位。"
                       TextWrapping="Wrap"
                       Foreground="{StaticResource TextSecondaryBrush}" />

            <TextBox x:Name="ExceptionTextBox"
                     Grid.Row="2"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     Text="{Binding ExceptionText, RelativeSource={RelativeSource AncestorType=Window}}" />

            <StackPanel Grid.Row="3"
                        Margin="0,16,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Width="110"
                        Margin="0,0,10,0"
                        Content="复制全部"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="OnCopyClick" />
                <Button Width="96"
                        IsDefault="True"
                        Content="关闭"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="OnCloseClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
