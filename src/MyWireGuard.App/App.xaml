<Application x:Class="MyWireGuard.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:MyWireGuard.App.Converters">
    <Application.Resources>

        <!-- ===================== BRUSHES ===================== -->
        <SolidColorBrush x:Key="WindowBrush"          Color="#020617" />
        <SolidColorBrush x:Key="SidebarBrush"         Color="#0F172A" />
        <SolidColorBrush x:Key="PanelBrush"           Color="#0B1120" />
        <SolidColorBrush x:Key="PanelElevatedBrush"   Color="#111827" />
        <SolidColorBrush x:Key="BorderBrush"          Color="#1E293B" />
        <SolidColorBrush x:Key="AccentBrush"          Color="#22C55E" />
        <SolidColorBrush x:Key="TextPrimaryBrush"     Color="#F8FAFC" />
        <SolidColorBrush x:Key="TextSecondaryBrush"   Color="#94A3B8" />
        <SolidColorBrush x:Key="StatusRunningBrush"   Color="#4CAF50" />
        <SolidColorBrush x:Key="StatusStartingBrush"  Color="#FFA726" />
        <SolidColorBrush x:Key="StatusStoppingBrush"  Color="#FF7043" />
        <SolidColorBrush x:Key="StatusStoppedBrush"   Color="#546E7A" />
        <SolidColorBrush x:Key="DangerBrush"          Color="#EF5350" />
        <SolidColorBrush x:Key="NavItemHoverBrush"    Color="#172033" />
        <SolidColorBrush x:Key="NavItemActiveBrush"   Color="#123525" />

        <!-- ===================== SYSTEM COLOR OVERRIDES (固定深色，不跟随系统主题) ===================== -->
        <Color  x:Key="{x:Static SystemColors.WindowColorKey}">#020617</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}"           Color="#020617" />
        <Color  x:Key="{x:Static SystemColors.WindowTextColorKey}">#F8FAFC</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}"       Color="#F8FAFC" />
        <Color  x:Key="{x:Static SystemColors.ControlColorKey}">#0B1120</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"          Color="#0B1120" />
        <Color  x:Key="{x:Static SystemColors.ControlTextColorKey}">#F8FAFC</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}"      Color="#F8FAFC" />
        <Color  x:Key="{x:Static SystemColors.HighlightColorKey}">#123525</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"        Color="#123525" />
        <Color  x:Key="{x:Static SystemColors.HighlightTextColorKey}">#F8FAFC</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"    Color="#F8FAFC" />
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"     Color="#111827" />
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#F8FAFC" />
        <Color  x:Key="{x:Static SystemColors.MenuColorKey}">#0F172A</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}"             Color="#0F172A" />
        <Color  x:Key="{x:Static SystemColors.MenuTextColorKey}">#F8FAFC</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}"         Color="#F8FAFC" />
        <Color  x:Key="{x:Static SystemColors.MenuHighlightColorKey}">#172033</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.MenuHighlightBrushKey}"    Color="#172033" />
        <Color  x:Key="{x:Static SystemColors.InfoColorKey}">#111827</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.InfoBrushKey}"             Color="#111827" />
        <Color  x:Key="{x:Static SystemColors.InfoTextColorKey}">#F8FAFC</Color>
        <SolidColorBrush x:Key="{x:Static SystemColors.InfoTextBrushKey}"         Color="#F8FAFC" />

        <!-- ===================== CONVERTER ===================== -->
        <converters:IntEqualsBoolConverter x:Key="IntEqualsBoolConverter" />

        <!-- ===================== TEXTBLOCK ===================== -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI" />
            <Setter Property="FontSize"   Value="13" />
        </Style>

        <!-- ===================== BUTTON TEMPLATE ===================== -->
        <ControlTemplate x:Key="FlatButtonTemplate" TargetType="Button">
            <Grid>
                <Border x:Name="ButtonBg"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="5" />
                <Border x:Name="HoverOverlay"
                        CornerRadius="5"
                        Background="White"
                        Opacity="0" />
                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                  Margin="{TemplateBinding Padding}" />
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.07" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="HoverOverlay" Property="Background" Value="Black" />
                    <Setter TargetName="HoverOverlay" Property="Opacity" Value="0.18" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="ButtonBg" Property="Opacity" Value="0.35" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ===================== BUTTON STYLES ===================== -->
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Template"                  Value="{StaticResource FlatButtonTemplate}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment"  Value="Center" />
            <Setter Property="Padding"                   Value="12,7" />
            <Setter Property="FontSize"                  Value="12" />
            <Setter Property="FontFamily"                Value="Microsoft YaHei UI" />
            <Setter Property="FontWeight"                Value="Normal" />
            <Setter Property="Cursor"                    Value="Hand" />
            <Setter Property="Foreground"                Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness"           Value="0" />
            <Setter Property="Background"                Value="{StaticResource PanelElevatedBrush}" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background"  Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground"  Value="White" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background"      Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush"     Value="{StaticResource BorderBrush}" />
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background"      Value="#3D1E1E" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush"     Value="{StaticResource DangerBrush}" />
            <Setter Property="Foreground"      Value="{StaticResource DangerBrush}" />
        </Style>

        <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrayMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuItemBg"
                                Margin="2"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                TextElement.Foreground="{TemplateBinding Foreground}">
                            <ContentPresenter ContentSource="Header"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemBg" Property="Background" Value="{StaticResource NavItemHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrayStatusMenuItemStyle" TargetType="MenuItem" BasedOn="{StaticResource TrayMenuItemStyle}">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Cursor" Value="Arrow" />
        </Style>

        <Style x:Key="TraySeparatorStyle" TargetType="Separator">
            <Setter Property="Margin" Value="8,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1"
                                Background="{StaticResource BorderBrush}"
                                SnapsToDevicePixels="True" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Default implicit button style -->
        <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}" />

        <!-- ===================== TEXTBOX ===================== -->
        <ControlTemplate x:Key="DarkTextBoxTemplate" TargetType="TextBox">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4"
                    Padding="{TemplateBinding Padding}">
                <ScrollViewer x:Name="PART_ContentHost"
                              VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                              HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                              Focusable="False" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="Bd" Property="Opacity" Value="0.5" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Template"       Value="{StaticResource DarkTextBoxTemplate}" />
            <Setter Property="Background"     Value="{StaticResource PanelElevatedBrush}" />
            <Setter Property="Foreground"     Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush"    Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush"     Value="{StaticResource AccentBrush}" />
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Padding"        Value="8,6" />
            <Setter Property="FontSize"       Value="13" />
        </Style>

        <!-- ===================== BADGES ===================== -->
        <Style x:Key="BadgeBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="10,3" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Background" Value="#3C4B61" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="success">
                    <Setter Property="Background" Value="#1FB85A" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="warning">
                    <Setter Property="Background" Value="#F08A24" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="danger">
                    <Setter Property="Background" Value="#E04848" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="neutral">
                    <Setter Property="Background" Value="#4E6077" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BadgeTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="SmallBadgeBorderStyle" TargetType="Border" BasedOn="{StaticResource BadgeBorderStyle}">
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="CornerRadius" Value="7" />
        </Style>

        <!-- ===================== NAV ITEM (RadioButton) ===================== -->
        <Style x:Key="NavItemStyle" TargetType="RadioButton">
            <Setter Property="Foreground"               Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize"                 Value="13" />
            <Setter Property="FontFamily"               Value="Microsoft YaHei UI" />
            <Setter Property="Cursor"                   Value="Hand" />
            <Setter Property="HorizontalAlignment"      Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FocusVisualStyle"         Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="NavBg"
                                Background="Transparent"
                                BorderThickness="3,0,0,0"
                                BorderBrush="Transparent"
                                Padding="20,11,16,11">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="NavBg" Property="Background"    Value="{StaticResource NavItemActiveBrush}" />
                                <Setter TargetName="NavBg" Property="BorderBrush"   Value="{StaticResource AccentBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBg" Property="Background" Value="{StaticResource NavItemHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== LISTBOX ===================== -->
        <Style TargetType="ListBox">
            <Setter Property="Background"                                Value="Transparent" />
            <Setter Property="BorderThickness"                           Value="0" />
            <Setter Property="Padding"                                   Value="0" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="FocusVisualStyle"                          Value="{x:Null}" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Background"       Value="Transparent" />
            <Setter Property="Padding"          Value="8,0" />
            <Setter Property="Margin"           Value="4,2,4,2" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBg" Property="Background" Value="{StaticResource PanelElevatedBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBg" Property="Background" Value="{StaticResource NavItemHoverBrush}" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected"  Value="True" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ItemBg" Property="Background" Value="{StaticResource PanelElevatedBrush}" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== DATAGRID ===================== -->
        <Style TargetType="DataGrid">
            <Setter Property="Background"              Value="Transparent" />
            <Setter Property="Foreground"              Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness"         Value="0" />
            <Setter Property="RowBackground"           Value="Transparent" />
            <Setter Property="AlternatingRowBackground" Value="#0A131E" />
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="VerticalGridLinesBrush"  Value="Transparent" />
            <Setter Property="ColumnHeaderHeight"      Value="34" />
            <Setter Property="SelectionMode"           Value="Single" />
            <Setter Property="SelectionUnit"           Value="FullRow" />
            <Setter Property="CanUserResizeRows"       Value="False" />
            <Setter Property="FocusVisualStyle"        Value="{x:Null}" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background"       Value="Transparent" />
            <Setter Property="Foreground"       Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PanelElevatedBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource NavItemHoverBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding"          Value="12,0" />
            <Setter Property="BorderThickness"  Value="0" />
            <Setter Property="Background"       Value="Transparent" />
            <Setter Property="Foreground"       Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background"   Value="Transparent" />
                    <Setter Property="BorderBrush"  Value="Transparent" />
                    <Setter Property="Foreground"   Value="{StaticResource TextPrimaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="{StaticResource PanelBrush}" />
            <Setter Property="Foreground"      Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize"        Value="11" />
            <Setter Property="FontWeight"      Value="SemiBold" />
            <Setter Property="Padding"         Value="12,0" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush"     Value="{StaticResource BorderBrush}" />
            <Setter Property="SeparatorBrush"  Value="{StaticResource BorderBrush}" />
        </Style>

        <Style TargetType="DataGridRowHeader">
            <Setter Property="Background"      Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width"           Value="0" />
        </Style>

    </Application.Resources>
</Application>
